# 问题日志

## 2026-09-09 — 新增暂停计时与提醒方式配置

- **需求：** 工作时段临时离开或不希望被打扰时，需要暂停并可继续当前工作计时；声音、Windows 系统通知和浏览器提醒弹窗应能分别开关。
- **实现：** 新增持久化的 `paused` 状态。暂停时不累计工作时间，也不触发提醒；恢复时在同一工作时段内延续进度。设置页新增三项独立提醒方式开关，默认全部开启。
- **验证：** 后台测试覆盖暂停/恢复以及关闭全部提醒方式时不产生声音、系统通知或浏览器弹窗。

## 2026-09-09 — 宁静氛围音乐仅播放一遍

- **现象：** 选择“宁静氛围音乐（CC0）”后，提醒铃声只播放一遍；其余声音可以持续播放。
- **排查：** 该资源 `sounds/calm-loop.mp3` 是约 19 秒的 MP3，带有 iTunes gapless（无缝播放）元数据。常规音源使用 `AudioBufferSourceNode.loop = true`；在用户的 Chromium 环境中，这一资源未能可靠进入下一轮。
- **修复：** 此资源改用链式播放：每一轮自然结束时立即创建下一个相同的音源，直到用户点击“知道了”或达到配置的播放时长。其余音频继续使用浏览器原生循环。
- **验证：** 自动测试模拟该资源自然结束，确认会创建下一轮音源；并确认停止消息会终止当前音源。
