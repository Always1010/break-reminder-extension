# 项目协作规范

## Git 提交策略

- 每次完成一个独立的代码修改任务后，先运行与修改相关的检查；检查通过后自动创建本地 Git commit。
- 不要自动执行 `git push`、`git pull`、合并、变基或强制推送，除非用户在当前请求中明确要求。
- 只提交当前任务实际修改的文件，不要使用会把用户既有修改一并纳入的盲目全量暂存操作。
- 如果工作区在任务开始前已有修改，保留这些修改，并只暂存本次任务涉及的文件。
- 如果检查失败，先修复问题；无法修复时不要创建“看似完成”的提交。
- 一个逻辑变更使用一个 commit；不要为了凑提交而拆分，也不要把不相关的修改合并到同一个提交。
- 提交前检查 `git diff --cached`，确认没有密钥、令牌、个人数据或无关文件。

## Commit message 格式

使用 Conventional Commits 格式：

```text
<type>(<scope>): <imperative summary>
```

允许的 `type`：`feat`、`fix`、`refactor`、`docs`、`test`、`chore`、`style`、`build`、`ci`、`perf`。

要求：

- 使用英文提交信息，简短、明确、使用动词原形。
- 标题不超过72个字符，不以句号结尾。
- 只有在确实需要时才填写 scope。
- 完成后向用户报告 commit hash、提交信息、检查结果，并明确说明未执行 push。

## 问题日志

- 使用项目根目录的 `ISSUE_LOG.md` 记录用户反馈的问题、排查到的原因、修复方案和验证结果。
- 每次修复问题时，必须在同一个本地 Git commit 中提交对应的问题日志更新和代码修改。
